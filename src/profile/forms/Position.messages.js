import { defineMessages } from '@edx/frontend-platform/i18n';

const messages = defineMessages({
  'profile.position.label': {
    id: 'profile.position.label',
    defaultMessage: 'Position',
    description: 'Label for position field',
  },
  'profile.position.empty': {
    id: 'profile.position.empty',
    defaultMessage: 'Position not specified',
    description: 'Text when position is empty',
  },
});

export default messages;